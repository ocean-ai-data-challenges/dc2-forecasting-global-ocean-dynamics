# git-lfs tracking for large leaderboard assets.
# map_data.tar.gz is generated by docs/scripts/pack_map_data.py after each
# evaluation run and committed here.  git-lfs keeps the binary out of the
# regular object store while still versioning it alongside the code.
docs/source/_extra/leaderboard/map_data.tar.gz filter=lfs diff=lfs merge=lfs -text
